/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "secure.gravatar.com",
      "mernblog-bucket.storage.iran.liara.space",
      "",
    ],
  },
};

module.exports = nextConfig;
